machine:
    java:
        version: openjdk7
    environment:
        ANDROID_HOME: /home/ubuntu/android
        JAVA_OPTS: "-Xmx2048m -XX:MaxPermSize=1024m"

test:
    override:
        - ./gradlew assembleDebug
